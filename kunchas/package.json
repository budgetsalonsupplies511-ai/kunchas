{
  "name": "kunchas",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Kunchas Cloudflare app for membership signups and salon admin.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute kunchas-db --local --file=./schema.sql && wrangler d1 execute kunchas-db --local --file=./services-upgrade.sql && wrangler d1 execute kunchas-db --local --file=./roster-upgrade.sql && wrangler d1 execute kunchas-db --local --file=./staff-no-default-branch.sql && wrangler d1 execute kunchas-db --local --file=./reporting-upgrade.sql && wrangler d1 execute kunchas-db --local --file=./time-break-upgrade.sql",
    "db:remote": "wrangler d1 execute kunchas-db --remote --file=./schema.sql && wrangler d1 execute kunchas-db --remote --file=./services-upgrade.sql && wrangler d1 execute kunchas-db --remote --file=./roster-upgrade.sql && wrangler d1 execute kunchas-db --remote --file=./staff-no-default-branch.sql && wrangler d1 execute kunchas-db --remote --file=./reporting-upgrade.sql && wrangler d1 execute kunchas-db --remote --file=./time-break-upgrade.sql",
    "db:booking-subcategory:local": "wrangler d1 execute kunchas-db --local --file=./booking-subcategory-upgrade.sql",
    "db:booking-subcategory:remote": "wrangler d1 execute kunchas-db --remote --file=./booking-subcategory-upgrade.sql",
    "db:manager-access:local": "wrangler d1 execute kunchas-db --local --file=./manager-branch-access-upgrade.sql",
    "db:manager-access:remote": "wrangler d1 execute kunchas-db --remote --file=./manager-branch-access-upgrade.sql",
    "db:manager-permissions:local": "wrangler d1 execute kunchas-db --local --file=./manager-permissions-upgrade.sql",
    "db:manager-permissions:remote": "wrangler d1 execute kunchas-db --remote --file=./manager-permissions-upgrade.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
