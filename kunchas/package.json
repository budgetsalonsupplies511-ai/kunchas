{
  "name": "kunchas",
  "version": "1.0.0",
  "private": true,
  "description": "Kunchas Cloudflare app for membership signups and salon admin.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute kunchas-db --local --file=./schema.sql && wrangler d1 execute kunchas-db --local --file=./services-upgrade.sql && wrangler d1 execute kunchas-db --local --file=./roster-upgrade.sql && wrangler d1 execute kunchas-db --local --file=./staff-no-default-branch.sql && wrangler d1 execute kunchas-db --local --file=./pos-operations-upgrade.sql",
    "db:remote": "wrangler d1 execute kunchas-db --remote --file=./schema.sql && wrangler d1 execute kunchas-db --remote --file=./services-upgrade.sql && wrangler d1 execute kunchas-db --remote --file=./roster-upgrade.sql && wrangler d1 execute kunchas-db --remote --file=./staff-no-default-branch.sql && wrangler d1 execute kunchas-db --remote --file=./pos-operations-upgrade.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}