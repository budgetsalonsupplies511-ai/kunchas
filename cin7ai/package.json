{
  "name": "cin7ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A standalone Cloudflare Worker dashboard for Cin7 Omni.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
